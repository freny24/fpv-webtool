# Copy this file to .env.local for local development (Vite auto-loads it).
# On Vercel, set VITE_API_URL as a Project > Environment Variable instead.

# URL of the deployed backend (Render). Leave unset locally to default to
# http://localhost:3001.
VITE_API_URL=http://localhost:3001
